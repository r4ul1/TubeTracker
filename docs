1-User-Story: User kann YouTube-Kanalnamen eingeben
- Wer handelt: Endbenutzer
- Wie wird gehandelt: Der Benutzer gibt den Namen eines YouTubers in ein Eingabefeld ein, das in der GUI des Programms angezeigt wird.
- Welche Daten werden benötigt: YouTube-Kanalname
- Wie werden die Daten transformiert und gespeichert: Die eingegebenen Daten werden in eine Variable gespeichert, die später verwendet wird,
  um eine Anfrage an die YouTube-API zu stellen.


2-User-Story: Anzeige der aktuellen Anzahl der Abonnenten
- Wer handelt: Endbenutzer
- Wie wird gehandelt: Das Programm ruft die aktuelle Anzahl der Abonnenten des eingegebenen Kanals ab und zeigt sie in der GUI an.
- Welche Daten werden benötigt: YouTube-Kanalname
- Wie werden die Daten transformiert und gespeichert: Die YouTube-API wird verwendet, um die Anzahl der Abonnenten des eingegebenen Kanals
  abzurufen und das Ergebnis wird in der GUI angezeigt.


3-User-Story: Aktualisierung der Abonnentenanzahl
- Wer handelt: Endbenutzer
- Wie wird gehandelt: Das Programm aktualisiert automatisch die Anzahl der Abonnenten oder der Benutzer kann die Anzahl der Abonnenten manuell über
  eine Schaltfläche aktualisieren.
- Welche Daten werden benötigt: YouTube-Kanalname
- Wie werden die Daten transformiert und gespeichert: Das Programm ruft regelmäßig die aktuelle Anzahl der Abonnenten ab oder der Benutzer kann
  die Anzahl der Abonnenten manuell durch Klicken auf eine Schaltfläche aktualisieren. Die aktuelle Anzahl der Abonnenten wird in der GUI angezeigt.


4-User-Story: Fehlerbehandlung
- Wer handelt: Endbenutzer
- Wie wird gehandelt: Das Programm überprüft die Gültigkeit des eingegebenen Kanalnamens und informiert den Benutzer, wenn der eingegebene Kanal nicht
  gefunden wird oder ungültig ist.
- Welche Daten werden benötigt: YouTube-Kanalname
- Wie werden die Daten transformiert und gespeichert: Das Programm überprüft die Gültigkeit des eingegebenen Kanalnamens und gibt eine Fehlermeldung aus,
  wenn der eingegebene Kanal nicht gefunden wird oder ungültig ist.


